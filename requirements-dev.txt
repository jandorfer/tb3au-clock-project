# Dev/test dependencies.
#
# The local test harness (tests/run_local_test.py + tests/test_local.py) mocks
# waveshare_epd / openai / paho, so it only needs Pillow + requests + pytest.
# NOTE: requirements.txt pins old versions (Pillow==8.1.2, requests==2.25.1,
# paho-mqtt==2.1.0, openai==2.6.1) for the Raspberry Pi's runtime / SDK
# compatibility. CI and local dev use current releases instead.
pytest>=7.0
Pillow>=10.0
requests>=2.28
pytest-cov>=4.0
